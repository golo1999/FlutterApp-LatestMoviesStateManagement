name: latest_movies_state_management
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  built_collection: ^5.1.0
  built_value: ^8.1.1
  cupertino_icons: ^1.0.3
  flutter_rating_bar: ^4.0.0
  flutter_redux: ^0.8.2
  freezed_annotation: ^0.14.3
  http: ^0.13.3
  json_annotation: ^4.1.0
  redux: ^5.0.0
  redux_epics: ^0.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.0.6
  built_value_generator: ^8.1.1
  flutter_lints: ^1.0.4
  freezed: ^0.14.2

dependency_overrides:
  rxdart: ^0.27.1

flutter:
  uses-material-design: true